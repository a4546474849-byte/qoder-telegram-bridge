{
  "name": "qoder-telegram-bridge",
  "version": "1.0.0",
  "description": "Telegram bot bridge for Qoder CLI — persistent sessions, streaming, full tool access",
  "main": "bridge.js",
  "scripts": {
    "start": "node bridge.js"
  },
  "dependencies": {
    "node-telegram-bot-api": "^0.66.0"
  }
}
